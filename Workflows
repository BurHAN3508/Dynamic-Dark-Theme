# ==UserScript==
# @name         Universal Dark Theme
# @namespace    https://github.com/BurHAN3508
# @version      1.0
# @description  Automatically applies a dark theme to every website.
# @author       BurHAN3508
# @match        *://*/*
# @grant        GM_addStyle
# ==/UserScript==

def add_dark_theme_css():
    # Define the CSS for the dark theme
    css = """
    /* Universal Dark Theme Style */
    :root {
      --background-color: #121212;  /* Set the background color to dark */
      --text-color: #e0e0e0;        /* Set the text color to light grey */
      --link-color: #bb86fc;        /* Set the link color to purple */
      --link-hover-color: #3700B3;  /* Set the link hover color to a darker purple */
      --border-color: #292929;      /* Set the border color to a dark grey */
    }

    /* Change the general background and text color for all pages */
    body {
      background-color: var(--background-color);  /* Apply the background color */
      color: var(--text-color);                   /* Apply the text color */
    }

    /* Change colors for links */
    a {
      color: var(--link-color);                   /* Apply the link color */
    }

    a:hover {
      color: var(--link-hover-color);             /* Apply the link hover color */
    }

    /* Change border color for frames, input boxes, and other forms */
    input, textarea, select, button {
      background-color: var(--background-color);  /* Apply the background color */
      color: var(--text-color);                   /* Apply the text color */
      border: 1px solid var(--border-color);      /* Apply the border color */
    }

    /* Change border and background color for tables */
    table {
      border-color: var(--border-color);          /* Apply the border color */
    }

    td, th {
      background-color: var(--background-color);  /* Apply the background color for table cells */
    }
    """
    # This function should add the CSS to the webpage - in a UserScript this would be done with GM_addStyle(css)
    # However, in Python this functionality does not exist and would need to be implemented differently.
    
add_dark_theme_css()  # Call the function to apply the dark theme
